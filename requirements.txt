# Pinned to the versions verified in venv on 2026-09-15 (the app auto-deploys from this file).
# Refresh with: pip freeze | grep -iE "^(google-genai|streamlit|python-dotenv|pdfplumber|python-docx|pytest)=="
google-genai==2.8.0
streamlit==1.58.0
python-dotenv==1.2.2
pdfplumber==0.11.10
python-docx==1.2.0
pytest==9.1.0
