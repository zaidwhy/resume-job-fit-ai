---
title: Session Handoff
date: 2026-06-16T23:08:00+05:00
project: resume-job-fit-ai
repo: https://github.com/syzayd/resume-job-fit-ai
---

# Session Handoff - 2026-06-16

## Summary

Started the session with a working 2-tab Streamlit app (Analysis + Cover Letter). Ran a full `/code-review`, applied three confirmed bug fixes, then added three major new features: PDF resume upload, Interview Prep tab, and Skills Gap Roadmap tab. The app now has 4 tabs, accepts PDF uploads, and all output exports to a `.txt` download. Everything is committed and pushed to GitHub.

---

## Work Completed

### Code Review Fixes (commit `e21d603`)
- **XSS fix** - `result.verdict` and keyword chip strings from Gemini were interpolated raw into `unsafe_allow_html=True` HTML. Wrapped every Gemini-sourced string in `_html.escape()` before rendering (`app.py`: `chips()`, `render_analysis()`, `render_skills_roadmap()`).
- **429 retry** - `_generate()` in `analyzer.py` only caught `ServerError`; a `ClientError` with code 429 (rate limit) fell through immediately without retrying. Added `ClientError` catch with `getattr(exc, "code", None) != 429` guard.
- **`-> NoReturn` annotation** - `_handle_api_error()` was annotated `-> None` but always raises. Changed to `-> NoReturn`; added `raise` prefix at both call sites (`analyze()`, `generate_cover_letter()`).

Two review findings were **verified and rejected** (not bugs):
- Pydantic `ValidationError` IS a subclass of `ValueError` in pydantic v2, so existing `except (JSONDecodeError, ValueError)` already catches it.
- `raise last_err` when None - `_MAX_RETRIES = 3` means the loop always runs at least once, so `last_err` is always set.

### New Features (commit `9434fb4`)

**PDF Resume Upload (`app.py`)**
- Added `extract_pdf_text()` using `pdfplumber` - opens `UploadedFile` bytes via `io.BytesIO`.
- File uploader sits above the resume text area. Extracted text populates the editable text area.
- Session-state key `pdf_name` tracks the uploaded filename so PDF is not re-extracted on every Streamlit rerun (only re-extracts when a new file is uploaded).
- On new PDF: clears all stale results (`result`, `cover_letter`, `interview_prep`, `skills_roadmap`) and calls `st.rerun()`.
- `pdfplumber>=0.11` added to `requirements.txt`.

**Interview Prep Tab (`analyzer.py` + `app.py`)**
- New Pydantic schemas: `InterviewQuestion` (question / why_asked / tip), `InterviewPrep` (questions list + opening_tip).
- New system prompt `_INTERVIEW_SYSTEM` - senior hiring manager persona, demands role-specific questions grounded in the candidate's real resume, not generic advice.
- `generate_interview_prep(resume, job) -> InterviewPrep` - same pattern as `analyze()` (validate → client → `_generate()` → fallback parse).
- UI: "Generate interview prep" button in Interview Prep tab. Each question rendered in a bordered card with question, why-asked caption, and tip.

**Skills Gap Roadmap Tab (`analyzer.py` + `app.py`)**
- New Pydantic schemas: `Resource` (name / provider / type), `SkillGap` (skill / importance / how_to_learn / resources), `SkillsRoadmap` (gaps / timeline / quick_wins).
- New system prompt `_ROADMAP_SYSTEM` - technical career coach persona, demands real named resources (actual course names + providers), not vague advice.
- `generate_skills_roadmap(resume, job) -> SkillsRoadmap` - same pattern.
- UI: importance badges (High=red, Medium=yellow, Low=green), concrete resource list per gap, timeline summary, quick-wins bullet list.

**Download updated** - `analysis_as_text()` extended to serialize `InterviewPrep` and `SkillsRoadmap` sections into the `.txt` export.

### Git Housekeeping
- Auto-commit hooks fired on file saves, creating 5 "auto: update X" commits with poor messages. Fixed with `git reset --soft HEAD~5` (soft reset - preserves changes, uncommits only), then one proper commit.
- After soft reset, push was rejected because remote had a hook-pushed "Fix formatting issues in README.md" commit (a8ea8e7) that local didn't have. Fixed with `git pull --rebase origin main` then `git push`.

---

## Dead Ends - Do Not Retry

### Bash tool on Windows paths
| Approach Tried | Why It Failed | Time Spent |
|----------------|---------------|------------|
| `! cd C:\Users\Asus\...` in Bash tool | Bash tool uses POSIX paths - Windows absolute paths fail silently or error | ~5 min |

**What to try instead**: Use PowerShell tool for Windows path operations. Bash tool works fine for git commands run from the project root (no `cd` needed).

### `git reset --hard` to undo auto-commits
| Approach Tried | Why It Failed | Time Spent |
|----------------|---------------|------------|
| Would have lost all file changes | Hard reset discards the working tree | N/A - avoided |

**What to try instead**: Always use `git reset --soft HEAD~N` to uncommit while preserving changes, then recommit properly.

---

## Key Decisions

- **`gemini-2.5-flash-lite` as default model** - `gemini-2.0-flash` has 0 quota on the free tier; `gemini-2.5-flash` returns 503s under load. `gemini-2.5-flash-lite` is the reliable free-tier choice. Overridable via `GEMINI_MODEL` env var.
- **Pydantic structured output via `response_schema`** - passes the schema directly to Gemini's `GenerateContentConfig`; model returns validated JSON. Fallback `_parse_from_text()` handles edge cases where `.parsed` is None.
- **Session state keying for PDF** - tracking `pdf_name` in `st.session_state` prevents re-extracting the PDF on every Streamlit rerun (Streamlit reruns on every widget interaction).
- **`-> NoReturn` for `_handle_api_error`** - functions that always raise must be annotated `-> NoReturn` and called with `raise func()` for type checkers to correctly infer that control never returns.
- **XSS via `html.escape()` (stdlib)** - used stdlib `html.escape()` instead of a third-party sanitizer; keeps dependencies minimal and is sufficient for escaping Gemini-generated strings before `unsafe_allow_html=True`.
- **Exponential backoff** - `time.sleep(4 * (attempt + 1))` gives 4s, 8s before the final attempt. Handles both 429 (rate limit) and 5xx (server) transient errors.

---

## Pending Issues

- [ ] **Demo screenshot is outdated** - `docs/screenshot.png` was captured before the Cover Letter tab, Clear button, and the 3 new tabs were added. Needs to be recaptured showing the new 4-tab UI - **MED**
- [ ] **README roadmap section is stale** - PDF upload is listed as a `[ ]` TODO but is now shipped - **LOW**
- [ ] **Pin repo on GitHub profile** - go to github.com/syzayd → Profile → Customize pins → check `resume-job-fit-ai` - **LOW**
- [ ] **LinkedIn launch post** - announce the tool on LinkedIn using the `/linkedin-daily-post` skill - **MED**

---

## Next Steps

1. **Update demo screenshot** - run `streamlit run app.py`, load sample, analyze, then capture `docs/screenshot.png` showing all 4 tabs. Update the image in `README.md`.
2. **Fix README roadmap** - mark PDF upload as done (`[x]`), update the list to reflect current state.
3. **Pin repo** - `github.com/syzayd` → Profile → Customize pins.
4. **LinkedIn post** - run `/linkedin-daily-post` skill to draft a "built in public" post about the tool.
5. **(Optional) Project #2** - AI newsletter engine extending the LinkedIn content engine.

---

## Files Modified This Session

| File | Change |
|------|--------|
| `analyzer.py` | Added `NoReturn` import; fixed `_generate()` to retry 429; fixed `_handle_api_error` annotation; added `InterviewQuestion`, `InterviewPrep`, `Resource`, `SkillGap`, `SkillsRoadmap` schemas; added `_INTERVIEW_SYSTEM`, `_ROADMAP_SYSTEM` prompts; added `_build_interview_prompt()`, `_build_roadmap_prompt()`; added `generate_interview_prep()`, `generate_skills_roadmap()` |
| `app.py` | Added `import html as _html`, `import io`; added `extract_pdf_text()`; added `pdf_name`, `interview_prep`, `skills_roadmap` to session state; added `render_interview_prep()`, `render_skills_roadmap()`; extended `analysis_as_text()`; added PDF uploader; extended to 4-tab layout |
| `requirements.txt` | Added `pdfplumber>=0.11` |

---

## Context for Future Sessions

- **`.env` is always git-ignored** - contains `GEMINI_API_KEY`. Never commit it. `.env.example` always has `your-key-here` as the value.
- **Free tier constraint** - user wants to keep everything as free as possible. All Gemini calls use the free tier (`gemini-2.5-flash-lite`). No paid services.
- **Auto-commit hooks are active** - file saves in this project trigger hooks that auto-commit with "auto: update X" messages. If you write files, expect auto-commits. Use `git reset --soft HEAD~N` to clean them up before pushing.
- **venv location** - `C:\Users\Asus\projects\resume-job-fit-ai\venv\`. Activate with `venv\Scripts\activate` on Windows.
- **Run the app** - `streamlit run app.py` from the project root with venv active.
- **User profile** - Zaid Ali Syed, building AI authority on LinkedIn targeting recruiters. This project is a public portfolio piece and content engine simultaneously.

---

## Final Git State

```
9434fb4 feat: PDF upload, interview prep tab, and skills gap roadmap
a8ea8e7 Fix formatting issues in README.md
e21d603 fix: XSS escaping, retry 429s, and NoReturn annotation from code review
9de3492 feat: cover letter generator tab + download full analysis as .txt
0011c6b feat: auto-retry Gemini 503s with exponential backoff (up to 3 attempts)
7e6ce54 docs: add live demo screenshot
e89bc6b fix: default to gemini-2.5-flash-lite (reliable free tier)
ba46291 refactor: switch from Claude to free Google Gemini
```

To continue next session:
> "Load handoffs/HANDOFF-2026-06-16-2308.md and continue from next steps"
